{
  /*import { Link } from "react-router-dom";

import {
  GalleryImgFive,
  GalleryImgFour,
  GalleryImgOne,
  GalleryImgSix,
  GalleryImgThree,
  GalleryImgTwo,
} from "../assets/images";

const HeroSection = () => {
  const heroImgs = [
    { id: 1, img: <GalleryImgOne className="hero-img" /> },
    { id: 2, img: <GalleryImgTwo className="hero-img" /> },
    { id: 3, img: <GalleryImgThree className="hero-img" /> },
    { id: 4, img: <GalleryImgFour className="hero-img" /> },
    { id: 5, img: <GalleryImgFive className="hero-img" /> },
    { id: 6, img: <GalleryImgSix className="hero-img" /> },
  ];
  return (
    <section className="hero-section">
      <div className="hero-section-holder sections-holder">
        <div className="hero-text-holder">
          <h1 className="hero-title">Zdrava voda. Pametan izbor</h1>
          <p className="hero-text">
            Čista voda susreće modernu tehnologiju. Doživite udobnost i
            održivost u svakom gutljaju.
          </p>
          <Link to="/aparati" className="hero-btn-link">
            <button className="hero-btn">Pogledajte našu ponudu</button>
          </Link>
        </div>
        <div className="carousel">
          <div className="column">
            {[...heroImgs.slice(0, 3), ...heroImgs.slice(0, 3)].map(
              (img, i) => (
                <div className="item" key={i}>
                  {img.img}
                </div>
              )
            )}
          </div>
          <div className="column">
            {[...heroImgs.slice(3, 6), ...heroImgs.slice(3, 6)].map(
              (img, i) => (
                <div className="item" key={i}>
                  {img.img}
                </div>
              )
            )}
          </div>
        </div>
      </div>
    </section>
  );
};

export default HeroSection;*/
}
